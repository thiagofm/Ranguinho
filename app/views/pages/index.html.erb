<% content_for :sidebar do %>
  <h4>Latest added rangos</h4>
  <ul class="unstyled">
    <% @latest_places.each do |place| %>
      <li><%= link_to place.title, place %></li>
    <% end %>
  </ul><br />
  <h4>Latest rangoers</h4>
  <ul class="unstyled">
    <% @latest_profiles.each do |profile| %>
      <li><%= link_to profile.user.username, profile_path(username: profile.user.username) %></li>
    <% end %>
  </ul><br />
<% end %>

<h2>
  <%= t('pages.index.hungry') %><br />
  <%= t('pages.index.cantcook') %><br />
  <%= t('pages.index.nomoney') %><br />
<h2>
<h1><%= t('pages.index.grubwithyourneighbor') %></h1>
<% if current_user.nil? %>
	<%= link_to t('site.header.signup'), new_user_registration_path, :class => 'btn btn-success' %>
<% end %>

<!-- Place this before your front page's body tag. -->
<% unless Rails.env == 'development' %>
  <script src="http://s0.railsrumble.com/banner.js" type="text/javascript"></script>
<% end %>
